{
  "name": "chiavo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run --workspace=apps/mobile",
    "web": "npm run --workspace=apps/web",
    "shared": "npm run --workspace=packages/shared"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "babel-preset-expo": "~54.0.10",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "expo": "54.0.33"
  }
}
